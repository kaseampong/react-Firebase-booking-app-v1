import React from 'react';

const PageHeader = (props) => (
    <div className="page-header">
    <h1 className="page-header__title text-center">{props.title}</h1>
</div>
);

export default PageHeader;
